tags:
  - Business Fields
operationId: getBusinessFieldsForProvider
summary: Business Fields Configuration for a Provider
description: >
  This endpoint gives you the list of all the Fields that are configured for a given Organization, grouped by section.
  Also tells you, for each field, if you have the permission to edit it or not.

  Only User roles PROVIDER or ADMIN can access this information.
parameters:
  - $ref: '../../components/index.yml#/parameters/path_provider_label'

responses:
  200:
    description: OK
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: '../../components/index.yml#/schemas/BusinessFields.Section'
  401:
    $ref: '../../components/index.yml#/responses/401'
  403:
    $ref: '../../components/index.yml#/responses/403'
  404:
    $ref: '../../components/index.yml#/responses/404'
